import React from 'react';

export default function Canvas() {
  return (
    <div className="w-full h-full bg-yellow-50 flex items-center justify-center">
      {/* Empty canvas - primary workspace area */}
    </div>
  );
}
